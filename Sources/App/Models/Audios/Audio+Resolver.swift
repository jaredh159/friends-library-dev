import Vapor

// below auto-generated
extension Resolver {
  func getAudio(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<Audio> {
    throw Abort(.notImplemented)
  }

  func getAudios(
    req: Req,
    args: NoArgs
  ) throws -> Future<[Audio]> {
    throw Abort(.notImplemented)
  }

  func createAudio(
    req: Req,
    args: Audio.GraphQL.Request.Args.Create
  ) throws -> Future<Audio> {
    throw Abort(.notImplemented)
  }

  func createAudios(
    req: Req,
    args: Audio.GraphQL.Request.Args.CreateMany
  ) throws -> Future<[Audio]> {
    throw Abort(.notImplemented)
  }

  func updateAudio(
    req: Req,
    args: Audio.GraphQL.Request.Args.Update
  ) throws -> Future<Audio> {
    throw Abort(.notImplemented)
  }

  func updateAudios(
    req: Req,
    args: Audio.GraphQL.Request.Args.UpdateMany
  ) throws -> Future<[Audio]> {
    throw Abort(.notImplemented)
  }

  func deleteAudio(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<Audio> {
    throw Abort(.notImplemented)
  }
}