import Vapor

// below auto-generated
extension Resolver {
  func getAudioPart(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<AudioPart> {
    throw Abort(.notImplemented)
  }

  func getAudioParts(
    req: Req,
    args: NoArgs
  ) throws -> Future<[AudioPart]> {
    throw Abort(.notImplemented)
  }

  func createAudioPart(
    req: Req,
    args: AudioPart.GraphQL.Request.Args.Create
  ) throws -> Future<AudioPart> {
    throw Abort(.notImplemented)
  }

  func createAudioParts(
    req: Req,
    args: AudioPart.GraphQL.Request.Args.CreateMany
  ) throws -> Future<[AudioPart]> {
    throw Abort(.notImplemented)
  }

  func updateAudioPart(
    req: Req,
    args: AudioPart.GraphQL.Request.Args.Update
  ) throws -> Future<AudioPart> {
    throw Abort(.notImplemented)
  }

  func updateAudioParts(
    req: Req,
    args: AudioPart.GraphQL.Request.Args.UpdateMany
  ) throws -> Future<[AudioPart]> {
    throw Abort(.notImplemented)
  }

  func deleteAudioPart(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<AudioPart> {
    throw Abort(.notImplemented)
  }
}