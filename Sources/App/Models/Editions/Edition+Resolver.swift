import Vapor

// below auto-generated
extension Resolver {
  func getEdition(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<Edition> {
    throw Abort(.notImplemented)
  }

  func getEditions(
    req: Req,
    args: NoArgs
  ) throws -> Future<[Edition]> {
    throw Abort(.notImplemented)
  }

  func createEdition(
    req: Req,
    args: Edition.GraphQL.Request.Args.Create
  ) throws -> Future<Edition> {
    throw Abort(.notImplemented)
  }

  func createEditions(
    req: Req,
    args: Edition.GraphQL.Request.Args.CreateMany
  ) throws -> Future<[Edition]> {
    throw Abort(.notImplemented)
  }

  func updateEdition(
    req: Req,
    args: Edition.GraphQL.Request.Args.Update
  ) throws -> Future<Edition> {
    throw Abort(.notImplemented)
  }

  func updateEditions(
    req: Req,
    args: Edition.GraphQL.Request.Args.UpdateMany
  ) throws -> Future<[Edition]> {
    throw Abort(.notImplemented)
  }

  func deleteEdition(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<Edition> {
    throw Abort(.notImplemented)
  }
}