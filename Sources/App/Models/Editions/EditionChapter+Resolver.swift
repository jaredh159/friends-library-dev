import Vapor

// below auto-generated
extension Resolver {
  func getEditionChapter(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<EditionChapter> {
    throw Abort(.notImplemented)
  }

  func getEditionChapters(
    req: Req,
    args: NoArgs
  ) throws -> Future<[EditionChapter]> {
    throw Abort(.notImplemented)
  }

  func createEditionChapter(
    req: Req,
    args: EditionChapter.GraphQL.Request.Args.Create
  ) throws -> Future<EditionChapter> {
    throw Abort(.notImplemented)
  }

  func createEditionChapters(
    req: Req,
    args: EditionChapter.GraphQL.Request.Args.CreateMany
  ) throws -> Future<[EditionChapter]> {
    throw Abort(.notImplemented)
  }

  func updateEditionChapter(
    req: Req,
    args: EditionChapter.GraphQL.Request.Args.Update
  ) throws -> Future<EditionChapter> {
    throw Abort(.notImplemented)
  }

  func updateEditionChapters(
    req: Req,
    args: EditionChapter.GraphQL.Request.Args.UpdateMany
  ) throws -> Future<[EditionChapter]> {
    throw Abort(.notImplemented)
  }

  func deleteEditionChapter(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<EditionChapter> {
    throw Abort(.notImplemented)
  }
}