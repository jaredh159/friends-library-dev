import Vapor

// below auto-generated
extension Resolver {
  func getFriendQuote(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<FriendQuote> {
    throw Abort(.notImplemented)
  }

  func getFriendQuotes(
    req: Req,
    args: NoArgs
  ) throws -> Future<[FriendQuote]> {
    throw Abort(.notImplemented)
  }

  func createFriendQuote(
    req: Req,
    args: FriendQuote.GraphQL.Request.Args.Create
  ) throws -> Future<FriendQuote> {
    throw Abort(.notImplemented)
  }

  func createFriendQuotes(
    req: Req,
    args: FriendQuote.GraphQL.Request.Args.CreateMany
  ) throws -> Future<[FriendQuote]> {
    throw Abort(.notImplemented)
  }

  func updateFriendQuote(
    req: Req,
    args: FriendQuote.GraphQL.Request.Args.Update
  ) throws -> Future<FriendQuote> {
    throw Abort(.notImplemented)
  }

  func updateFriendQuotes(
    req: Req,
    args: FriendQuote.GraphQL.Request.Args.UpdateMany
  ) throws -> Future<[FriendQuote]> {
    throw Abort(.notImplemented)
  }

  func deleteFriendQuote(
    req: Req,
    args: IdentifyEntityArgs
  ) throws -> Future<FriendQuote> {
    throw Abort(.notImplemented)
  }
}