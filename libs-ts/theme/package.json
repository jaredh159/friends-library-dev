{
  "name": "@friends-library/theme",
  "version": "1.2.0",
  "description": "friends library theme, color, etc.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "pnpm tsc --noEmit",
    "test": "true",
    "lint": "pnpm eslint .",
    "lint:fix": "pnpm eslint . --fix",
    "format": "pnpm prettier --write .",
    "format:check": "pnpm prettier --check ."
  },
  "devDependencies": {
    "@friends-library/types": "workspace:*"
  }
}
