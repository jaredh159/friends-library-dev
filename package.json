{
  "name": "@friends-library/theme",
  "version": "1.1.1",
  "description": "friends library theme, color, etc.",
  "main": "./dist/index.js",
  "browser": "./src/index.ts",
  "author": "Jared Henderson <jared@netrivet.com>",
  "repository": "github:friends-library-dev/theme",
  "license": "MIT",
  "sideEffects": false,
  "scripts": {
    "test": "true",
    "lint": "fldev lint",
    "lint:fix": "fldev lint:fix",
    "ts:check": "fldev ts:check",
    "format": "fldev format",
    "compile": "fldev ts:compile",
    "prepublishOnly": "npm run compile",
    "npub:precheck": "fldev ci"
  },
  "devDependencies": {
    "@friends-library/dev": "^4.4.1",
    "@friends-library/types": "^6.0.0"
  }
}
